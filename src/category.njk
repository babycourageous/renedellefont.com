---
layout: base
pagination:
  data: collections.categoryList
  size: 1
  alias: category
permalink: /categories/{{category}}/
---

<div class="max-w-xl mx-auto">

  {% set categorylist = collections[category] %}
  <h1 class="text-2xl leading-none">Posts about <span class="text-5xl text-green-600 ">{{ category }}</span></h1>
  <p class="mt-6">
    There are {{ categorylist.length }} articles about <em>{{ category }}</em> :</p>
</p>

<ul class="mt-6 o-10">
  {% for post in categorylist | reverse %}
    <li>{%- include "post-preview-dark.njk" -%}</li>
  {% endfor %}
</ul>

<p class="mt-3">
  <a class="underline" href="/categories"><< Back to the category list</a>
</p>
</div>